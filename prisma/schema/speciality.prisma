model Speciality {
  id String @id @default(uuid(7))

  title       String  @unique @db.VarChar(100)
  description String? @db.Text
  icon        String? @db.VarChar(100)

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  isDeleted          Boolean            @default(false)
  deletedAt          DateTime?
  doctorSpecialities DoctorSpeciality[]

  @@index([isDeleted], name: "idx_speciality_isDeleted")
  @@index([title], name: "idx_speciality_title")
  @@map("specialities")
}

model DoctorSpeciality {
  id           String     @id @default(uuid(7))
  doctorId     String
  specialityId String
  doctor       Doctor     @relation(fields: [doctorId], references: [id], onDelete: Cascade, onUpdate: Cascade)
  speciality   Speciality @relation(fields: [specialityId], references: [id], onDelete: Cascade, onUpdate: Cascade)

  @@unique([doctorId, specialityId], name: "unique_doctor_speciality")
  @@index([doctorId], name: "idx_doctor_specialities_doctorId")
  @@index([specialityId], name: "idx_doctor_specialities_specialityId")
  @@map("doctor_specialities")
}
